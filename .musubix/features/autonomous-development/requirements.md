---
schemaVersion: 1
feature: autonomous-development
---
# MUSUBIX4 autonomous development / MUSUBIX4自律開発

## REQ-AUTONOMOUS-DEVELOPMENT-001: Preserve the MUSUBIX3 CLI surface / MUSUBIX3 CLI互換
Priority: must
Type: functional
Pattern: ubiquitous
Statement: The system shall expose every command and option supported by `nahisaho/musubix3` commit `c0b20f06727bceb04eeec181d95af9047b1981de` under the `musubix4` executable and preserve the inherited commands' observable behavior.
Acceptance: The repository shall contain `baseline-specs/baseline.manifest.json`, identifying the upstream repository and commit, the identifier and version of a deterministic regeneration procedure, and SHA-256-pinning an upstream-only command-surface snapshot, behavioral golden outputs, the inherited compatibility test/fixture set, a historical trace mapping, and a digest-pinned canonical regeneration attestation generated from that upstream commit; the regeneration procedure is an out-of-run maintainer or CI operation with separately authorized upstream access and reproduces byte-identical oracle artifacts, the same canonical attestation digest, and 0 upstream-manifest-entry differences for the pinned commit and procedure version; each run verifies locally that the attestation identifies the pinned upstream commit and procedure version and that all recorded oracle digests match the frozen artifacts, failing with a typed diagnostic otherwise; `baseline-specs/**` and manifest-enumerated compatibility tests and fixtures cannot be amended within a run, and changing them requires out-of-run regeneration, renewed requirements and design approval, and a new run; automated compatibility tests compare MUSUBIX4 only with those verified frozen oracle artifacts and report 0 missing commands, 0 missing options, and 0 mismatches in exit status, machine-readable output keys, or typed diagnostic codes; every command and option in the pinned upstream surface snapshot is exercised by at least 1 compatibility case; `musubix4 --version` identifies MUSUBIX4 rather than MUSUBIX3.

## REQ-AUTONOMOUS-DEVELOPMENT-002: Preserve MUSUBIX3 artifacts / MUSUBIX3成果物互換
Priority: must
Type: functional
Pattern: event-driven
Statement: When a repository contains MUSUBIX3 schema-version-1 `.musubix` artifacts accepted by the vendored baseline, the system shall validate and use them without requiring migration.
Acceptance: The inherited compatibility set enumerated with path and SHA-256 in `baseline-specs/baseline.manifest.json` covers requirements, design, constitution, trace, graph, TDD, approval, evidence, gate, status, workflow, mutation, model correspondence, attestation, and change records and passes through MUSUBIX4 with no fixture rewrite; the pinned historical trace mapping maps every requirement ID in `baseline-specs/features/**/requirements.md` to at least 1 inherited implementation path and at least 1 executing compatibility test path while active MUSUBIX4 annotations normalize those links to REQ-AUTONOMOUS-DEVELOPMENT-001 with 0 dangling trace IDs; MUSUBIX4 continues to emit the baseline schemas for inherited commands, while MUSUBIX4-only HoH artifacts are explicitly outside MUSUBIX3 forward compatibility.

## REQ-AUTONOMOUS-DEVELOPMENT-003: Freeze the public specification / 公開仕様の固定
Priority: must
Type: functional
Pattern: event-driven
Statement: When a run starts, the system shall materialize prompt text, a Markdown file, a GitHub Issue, or existing `.musubix` requirements as immutable untrusted specification data with its source and digest.
Acceptance: Each of the 4 input kinds produces a run-local specification file no larger than `maxSpecificationBytes` with a default of 1048576 bytes and containing source kind, pinned source locator, SHA-256 digest, and normalized content; prompt text uses a persisted run-start nonce plus run ID plus digest as its locator and reuses that locator on resume; normalized input exceeding the limit fails with a typed diagnostic before the first role invocation and is never truncated; exactly once at freeze time, a deterministic versioned normalizer produces and digest-pins at most `maxRequirementEntries`, defaulting to 200, entries containing stable ID, obligation text, and executable acceptance cases, using declared IDs and cases when present and content-derived IDs and deterministic cases otherwise; an entry with no executable case or input exceeding the entry limit fails with a typed diagnostic before the first role invocation; every iteration reuses that byte-identical index; later role invocations receive byte-identical content framed by a per-role-invocation nonce-derived delimiter that does not occur in the content; fixtures embedding candidate delimiters and role-instruction text cause 0 changes to effective permissions, mandatory QA dimensions, or readiness criteria.

## REQ-AUTONOMOUS-DEVELOPMENT-004: Fix the harness-model configuration / ハーネス・モデル構成の固定
Priority: must
Type: functional
Pattern: event-driven
Statement: When a run starts or resumes, the system shall enforce and meter one fixed Copilot model, reasoning configuration, Copilot CLI version, and positive AI-credit budget across every role invocation.
Acceptance: Starting without a model or positive budget fails before invoking Copilot; before spawning, each invocation durably reserves `roleInvocationReservedCredits`, defaulting to 1 positive AI credit, then charges the full non-negative parsed usage when available, while interruption or unparseable or missing usage consumes the full reservation; parsed usage above the reservation is charged in full, records a typed reservation-overrun diagnostic, and records `budget-exhausted` immediately when total usage exceeds the budget; no later invocation starts after exhaustion; no invocation starts when its reservation would exceed the budget; each invocation is terminated at `roleInvocationTimeoutMs`, defaulting to 120000 milliseconds, and timeout or transport failure is recorded as a typed abandoned attempt; the role-output parser selects the current protocol when at least 1 `session.usage_checkpoint` or `assistant.message` with `data.phase` equal to `final_answer` exists, otherwise selects the legacy protocol, whose `usage` event and `result.result` payload remain mandatory; every completely captured well-formed JSON event appears in evidence in received order after secret redaction, but a process record marked `truncated` is rejected with an output-bound error before parsing its potentially partial JSONL and consumes the full reservation; only the selected protocol's explicit semantic anchors can affect model identity, usage, role output, or success; in the current protocol each `session.usage_checkpoint.data.totalNanoAiu` must be a non-negative safe integer no lower than the preceding checkpoint, the last checkpoint is compared as integer nano-AIU against the configured budget and reservation converted with `Math.trunc(credits * 1000000000)`, and division by 1000000000 occurs only for returned display, while absence of a checkpoint charges the full reserved amount; every final-answer `assistant.message.data.model` equals the configured model, the last such message supplies one complete JSON role output through `data.content`, and exactly 1 `result` event must be the final well-formed event with top-level `exitCode` equal to 0; other event types are evidence-only and can neither contribute usage nor satisfy model, role-output, or result requirements; malformed JSON, an unclassified legacy stream, invalid or decreasing current usage, missing or invalid selected role output, model drift, a non-final or unsuccessful result, and ambiguous legacy results are rejected; the Copilot CLI version is verified by probing the same executable path before invocation, and the configured model and reasoning are fixed in the recorded process arguments even when the current stream exposes no reasoning field; every role invocation uses a new non-resumed Copilot session; resume refuses unavailable models or CLI-version drift with a typed diagnostic; every invocation record has identical configuration identifiers.

## REQ-AUTONOMOUS-DEVELOPMENT-005: Produce bounded plans / 小さく検証可能な計画
Priority: must
Type: functional
Pattern: event-driven
Statement: When an iteration begins, the Planner shall use only the public specification and preceding evidence to select no more than three ordered priorities with applicable repairs, an applicable capability increment, preservation constraints, and observable acceptance gates.
Acceptance: Planner output that has more than 3 priorities, omits a reported blocker or regression, omits an increment while unimplemented public requirements remain, or lacks preservation and acceptance gates is rejected and retried; when no repair or increment remains, the only valid plan outcome is a readiness proposal.

## REQ-AUTONOMOUS-DEVELOPMENT-006: Develop incrementally without reset / 検証済み機能を保つ増分開発
Priority: must
Type: functional
Pattern: event-driven
Statement: When the Developer receives an approved development document, the system shall instruct it to update the existing workspace, repair build or runtime blockers first, preserve verified behavior, implement the bounded increment, and produce execution records.
Acceptance: The Developer invocation receives the current artifact and development document; verified behavior includes the inherited compatibility checks in REQ-AUTONOMOUS-DEVELOPMENT-001 and REQ-AUTONOMOUS-DEVELOPMENT-002; a candidate cannot enter QA when configured build, focused test, or inherited compatibility commands fail or required execution records are absent.

## REQ-AUTONOMOUS-DEVELOPMENT-007: Isolate independent QA / 独立QAの隔離
Priority: must
Type: functional
Pattern: event-driven
Statement: When a candidate enters QA, the system shall create a complete Git-backed snapshot and invoke a separate QA Tester in a disposable worktree whose tracked source is non-writable and whose declared output directories are writable.
Acceptance: The candidate commit includes every Developer-produced source file; snapshot creation fails if source changes remain uncommitted outside declared disposable paths; the snapshot contains tracked files plus paths selected by `candidateExtraPaths`, which defaults to an empty list; at snapshot creation the system records the candidate commit's lockfile path and digest when present, verifies the worktree lockfile against that digest before provisioning, and treats a mismatch as a typed failure; dependencies are provisioned into a declared writable disposable path from that lockfile by a bounded recorded adapter command, without lifecycle scripts unless explicitly approved; a project with no lockfile skips provisioning but records that fact and must still pass its configured commands; the fresh QA worktree is runnable only when configured build and focused-test commands succeed there; the QA input commit equals the candidate commit; attempted tracked-source writes fail; build outputs, caches, logs, and dependency directories can use declared disposable paths; the active Developer workspace remains unchanged; the worktree is removed after evidence capture.

## REQ-AUTONOMOUS-DEVELOPMENT-008: Require claim-evidence QA / 主張と証拠に基づくQA
Priority: must
Type: functional
Pattern: ubiquitous
Statement: The system shall independently assess functional correctness, completeness, regression safety, usability, security, performance, and maintainability and derive readiness using structured claim-evidence records from public requirements and observable execution.
Acceptance: Every frozen requirement-index entry and each of the 7 quality dimensions has exactly 1 claim with a deterministic ID derived from that pair and stable across iterations; the dimension rubric is deterministic and universally applicable: functional correctness requires the entry's executable acceptance cases to pass, completeness requires every stated obligation to have executing evidence, regression safety requires the run-level result for all previously verified and inherited compatibility cases to remain passing and is attributed to every requirement, usability requires every affected command and option to appear in generated help and a candidate-local MUSUBIX4 command-surface snapshot and every acceptance failure to produce an actionable typed diagnostic, security requires the acceptance execution to add no undeclared capability and expose no named secret, performance requires every acceptance execution to finish within `commandTimeoutMs` and `maxCommandOutputBytes` plus any stricter stated bound, and maintainability requires the requirement's traced artifacts to pass the mandatory static-validation, build, trace, dependency-cycle, and structured-contract checks; other repository-global evidence is attributed only to claims whose traced artifacts participate in the check; every claim has exactly 1 `verified`, `gap`, or `insufficient-evidence` status and cited public evidence satisfying its rubric; the verified and non-verified sets are disjoint and cover all claims; source-code presence alone cannot verify a claim; a whole dimension can be removed only by the run-start frozen effective configuration and rationale covered by the initial REQ-AUTONOMOUS-DEVELOPMENT-016 requirements approval, the dimension set cannot change in-run, and the removed dimension creates no claims and does not suppress or skip any configured command; readiness requires every applicable claim to be `verified` with 0 gaps and 0 insufficient-evidence records.

## REQ-AUTONOMOUS-DEVELOPMENT-009: Transfer only artifact and evidence state / 反復間状態の限定
Priority: must
Type: functional
Pattern: event-driven
Statement: When an iteration completes, the system shall carry the accepted artifact and normalized QA evidence into the next iteration while generating a new development document instead of persisting the previous document as planner state.
Acceptance: The accepted artifact is the candidate unless REQ-AUTONOMOUS-DEVELOPMENT-018 applies, in which case it is the last preservation-verified candidate or the run base commit when none exists; before each Planner invocation the Planner claim-summary index capacity is the greater of 200 and the current non-verified claim count; iteration `t+1` Planner inputs list every non-verified claim before verified claims, never truncate a non-verified claim, may omit only trailing verified summaries after the capacity is reached, exclude the iteration `t` development document, and identify verified preservation constraints and unresolved update targets; detailed immutable records and the complete claim set remain retrievable by index reference.

## REQ-AUTONOMOUS-DEVELOPMENT-010: Enforce structured role contracts / ロール出力契約
Priority: must
Type: functional
Pattern: unwanted-behavior
Statement: If a role invocation returns invalid output, then the system shall reject the output and retry within the configured retry limit without advancing the iteration.
Acceptance: Invalid output, timeout, and transport-failure fixtures for each role, including Reviewer, advance neither iteration nor candidate refs; `roleOutputRetryLimit` defaults to 2 retries and applies within one role invocation at one workflow step; each failed attempt is durably marked abandoned before retry; for Reviewer, exhaustion of these role-output retries or valid but inadmissible review evidence consumes exactly 1 REQ-AUTONOMOUS-DEVELOPMENT-016 approval-evaluation attempt, while other role exhaustion records its typed terminal failure; a valid retry advances exactly once.

## REQ-AUTONOMOUS-DEVELOPMENT-011: Provide resumable lifecycle commands / 再開可能なライフサイクル
Priority: must
Type: functional
Pattern: ubiquitous
Statement: The system shall provide run, resume, status, and stop lifecycle capabilities under the exact invocation names pinned in `.musubix/compatibility/command-collisions.json`, backed by durable run state and a single-writer lock.
Acceptance: A process interruption after every persisted transition resumes without repeating a completed role or skipping an incomplete role; interruption during a role restores the last internal ref, marks the partial invocation abandoned, and reruns that role; a live concurrent writer is rejected; a stale lock owned by a dead process is reclaimed with an audit record; the effective stop invocation reaches a resumable stopped state; the effective status invocation reports the current run, iteration, role, limits, usage, evidence summary, stop trigger, resumability, amendment episode count, current amendment failure count, current approval boundary identity, consumed and remaining approval attempts, required operator action, offending inventory paths, and terminal reason when terminal; the MUSUBIX4-owned `.musubix/compatibility/command-collisions.json` compares the frozen upstream surface with the candidate-local MUSUBIX4 surface and pins exactly 1 classification and reachable invocation for every candidate top-level command; one versioned command-surface extraction contract produces both the deterministic pre-QA declared path/option set and the QA-time declared path/option set, and any divergence between those declared sets is a typed candidate failure; immediately after candidate commit creation, the static surface evaluates inventory structure and path completeness before QA worktree provisioning; a missing candidate-path entry, duplicate or conflicting entry, structurally malformed inventory, or extra stale entry whose path is absent from both frozen upstream and candidate surfaces suppresses claim assignment for that iteration and produces a non-destructive `amendment-required` stop that retains the original candidate commit and consumes no blocker-repair budget; adding a candidate-only top-level command intentionally requires this operator-initiated amendment safety cycle; an approved inventory change creates a provisional MUSUBIX4-owned amendment commit whose only delta from the retained candidate is the approved collision inventory, re-runs static extraction and approved-digest verification, and becomes the candidate, QA input commit, and REQ-AUTONOMOUS-DEVELOPMENT-018 preservation unit only after those checks succeed; an upstream-only path missing from the candidate remains an inherited compatibility regression; QA separately verifies the pinned invocation is executable after dependency provisioning, and a present but incorrect or unreachable resolution completes the full claim set with gaps in the affected usability claims, while inherited compatibility independently produces regression-safety gaps when applicable; after successful amendment, QA worktree provisioning and QA execute exactly once for the amendment commit; automated cases confirm the inherited invocation form retains the pinned exit status and output keys and the HoH capability is reachable only through the pinned classification.

## REQ-AUTONOMOUS-DEVELOPMENT-012: Stop deterministically / 決定的な停止
Priority: must
Type: functional
Pattern: event-driven
Statement: When a run stop trigger occurs, the system shall stop role iteration and record the trigger before entering any configured completion sequence.
Acceptance: Stop triggers are REQ-AUTONOMOUS-DEVELOPMENT-008 readiness, release-stage `approval-blocked`, cancellation, 30 completed iterations by default, 24 active non-paused hours by default, budget exhaustion, 3 stagnant iterations by default, `amendment-required`, and unrecoverable typed failure; active duration is wall-clock time during which a role invocation or adapter command is eligible to run, including Reviewer invocations and approval-boundary gate execution, while only waiting between automatic approval attempts during which no role invocation or adapter command is eligible to run, `amendment-required` stop, and stopped state is non-active; `amendment-required` retains the candidate, assigns no claims, does not complete an iteration or increment stagnation, rejects plain resume with a typed diagnostic, and resumes the same iteration only after the operator issues the explicit collision-inventory amendment command and the approved amendment completes; `amendmentRetryLimit` defaults to 2 retries after the initial amendment attempt and counts failed attempts across one `amendment-required` episode, while `maxAmendmentEpisodes` defaults to 3 per run; both counters are durable run state, survive process interruption, stop, and resume, and a new operator request or newly approved amendment manifest resets neither; the failure counter resets only after a successful amendment leaves that state, while the episode counter increments on each entry into a new `amendment-required` episode; any attempt whose verified automatic amendment approval is blocked, that fails pre-commit digest verification, or whose provisional amendment commit fails static extraction or approved-digest verification discards any provisional commit, retains the original candidate, consumes exactly 1 amendment attempt, and returns to `amendment-required` unless the amendment retry limit is exhausted; the second retry's failure, which is the third failed attempt by default, or entry into a fourth amendment episode by default, records an unrecoverable typed failure; a Planner readiness proposal while any applicable claim is not verified is rejected and retried without firing the readiness trigger; improvement is computed over the complete untruncated claim set and means either a strictly higher count of stable claim IDs with `verified` status, a strictly lower count with `gap` or `insufficient-evidence` status than the preceding iteration, or, during post-readiness release repair, verified resolution of at least one carried release blocker without introducing any regression; readiness records a non-terminal stop trigger and normally transfers control to REQ-AUTONOMOUS-DEVELOPMENT-017 rather than starting another role, but the exclusive release-stage `release-repair-required` outcome defined by REQ-AUTONOMOUS-DEVELOPMENT-016 clears that trigger without operator action, resets the stagnation counter to 0, and re-enters the bounded Planner-Developer-QA path in the same run; the retained pre-repair claim matrix remains the regression and improvement baseline but is no longer approval authority, and each post-readiness repair iteration follows the normal iteration, active-duration, budget, regression, improvement, and stagnation rules plus the remaining release approval-attempt limit; status reports the cleared readiness trigger and automatic resumability while this repair is active; deterministic tests cover every trigger at the exact boundary; limits are configurable and a positive AI-credit budget remains mandatory.

## REQ-AUTONOMOUS-DEVELOPMENT-013: Enforce role-specific least privilege / ロール別最小権限
Priority: must
Type: functional
Pattern: ubiquitous
Statement: The system shall invoke Copilot and project subprocesses through a recorded role-specific least-privilege policy that denies unconfigured capabilities by default.
Acceptance: Copilot tool calls enforce allowlisted tools, paths, URLs, MCP servers, and custom instructions; `baseline-specs/**`, `.musubix/compatibility/command-collisions.json`, and every inherited compatibility test and fixture path enumerated in `baseline-specs/baseline.manifest.json` are immutable input for every role; any enumerated protected path that is missing or unreadable at run start is a typed configuration failure that prevents the first Planner invocation; the effective protected-set digest recorded at run start or most recently re-pinned by an approved amendment must match at each role boundary, QA snapshot creation, and candidate acceptance, and later disappearance is an unrequested mismatch; in-run amendment is limited to `.musubix/compatibility/command-collisions.json`; an explicit CLI request prepares an amendment manifest listing the replacement collision-inventory path and SHA-256, and the new current REQ-AUTONOMOUS-DEVELOPMENT-016 requirements approval pins that manifest's digest; every amendment attempt verifies actual content against the approved manifest before and after creating a provisional amendment commit; any verification failure follows REQ-AUTONOMOUS-DEVELOPMENT-012, while successful validation atomically re-records the effective protected-set digest from the amendment commit, binds it to the approving requirements approval, and makes all later boundary comparisons use that most recent pin; an unrequested mismatch at candidate acceptance is a REQ-AUTONOMOUS-DEVELOPMENT-018 regression, while an unrequested mismatch at any earlier boundary records an unrecoverable typed failure and stops under REQ-AUTONOMOUS-DEVELOPMENT-012; Planner cannot modify files or execute project commands; Reviewer has a manifest-path read allowlist, cannot modify files, execute project commands, use producer conversation state, or access paths outside its exact review input; QA cannot modify tracked production files, and its environment, cache access, and optional network access for the lockfile-based provisioning command are independently allowlisted and recorded in `residualRisks`; Developer subprocesses are confined to the target working directory with an environment allowlist and configured network policy; named secrets are removed or redacted from process environments, prompts, logs, stored records, and evidence; every invocation records the effective policy and a `residualRisks` array naming each capability explicitly allowlisted beyond the role default.

## REQ-AUTONOMOUS-DEVELOPMENT-014: Preserve versioned project history / 版管理された履歴
Priority: must
Type: functional
Pattern: event-driven
Statement: When a role completes successfully, the system shall preserve its artifact and execution record under MUSUBIX4-owned internal Git history without changing the user's current branch.
Acceptance: Run metadata, role prompts, structured outputs, command records, usage, evidence summaries, state transitions, and artifact references have schema version, timestamp, run ID, iteration, input digests, and output digest; Planner, Developer, and QA stages create internal refs linked to those records; run-local telemetry is excluded from candidate source snapshots; the checked-out user branch name and pre-run uncommitted changes remain unchanged.

## REQ-AUTONOMOUS-DEVELOPMENT-015: Run generic and Node.js project adapters / 実行アダプター
Priority: must
Type: functional
Pattern: event-driven
Statement: When project commands are configured or a Node.js or TypeScript project is detected, the system shall execute bounded build, test, run, and evidence-collection commands through a versioned adapter contract.
Acceptance: Generic command fixtures preserve argument boundaries, contained working directory, `commandTimeoutMs` with a default of 120000 milliseconds, `maxCommandOutputBytes` with a default of 1048576 bytes, exit status, stdout, and stderr; every adapter must configure or deterministically discover build, focused-test, static-validation, trace, dependency-cycle, and structured-contract checks before the first Planner invocation, and absence of any check is a typed configuration failure that prevents run start; a configured check that cannot execute during evaluation yields `insufficient-evidence`, never `verified`; Node.js fixtures deterministically discover package-manager scripts without executing dependency installation or lifecycle scripts; a run with neither configured commands nor supported discovery fails before the first Planner invocation; unsupported projects receive a configuration diagnostic.

## REQ-AUTONOMOUS-DEVELOPMENT-016: Automate only verified approvals / 検証済み承認のみ自動化
Priority: must
Type: functional
Pattern: event-driven
Statement: When an approval boundary is reached, the system shall automatically approve only after all stage-specific validation, independent review, and mandatory quality evidence succeeds, with the decision bound to the current run and exact artifact manifest digest.
Acceptance: Every run policy automatically evaluates canonical `requirements`, `design`, and `release` stages without interactive confirmation and enforces identical non-weakenable evidence preconditions; a collision-inventory amendment is a new `requirements`-stage boundary with `boundaryKind: amendment`, the durable amendment episode ordinal, and the amendment attempt ordinal, not a fourth approval stage; each amendment attempt opens a distinct approval boundary keyed by `(run ID, requirements, amendment, amendment episode ordinal, amendment attempt ordinal)`, so the default 3 amendment episodes with 3 amendment attempts each permit at most 9 amendment approval boundaries, each with its own approval-attempt pool, and a successful approval closes only that amendment-attempt boundary while any later commit validation failure returns to `amendment-required` and opens the next amendment-attempt boundary; requirements and amendment requirements approval require current manifest-listed artifacts that pass schema and EARS checks plus stage-scoped trace validation of unique well-formed requirement IDs, design approval additionally requires complete requirement-to-design linkage over its manifest, and release approval requires full strict requirement-to-design-to-code-to-test trace, readiness, every frozen claim verified, and successful execution in a fresh isolated checkout of the accepted candidate digest of build, focused-test, static-validation, trace, dependency-cycle, structured-contract, and inherited-compatibility gates started after the claim matrix was frozen, with each gate record containing the executed tree digest; any absent or non-executable gate yields a typed configuration failure or `insufficient-evidence`, never approval, and retries may reuse gate records only while the candidate digest and claim-matrix digest remain unchanged; each boundary records a stage-entry tree digest, computes the changed-path set against that digest excluding declared disposable paths, and requires the stage manifest to contain its mandatory artifact set plus every changed path, where requirements includes the constitution, all active requirements, frozen effective run configuration including every waiver rationale, and for amendment both the amendment manifest and replacement collision-inventory bytes, design additionally includes every active design and linked ADR, and release includes the accepted candidate identity, complete claim matrix, all mandatory command records, trace evidence, and deployment configuration, so a missing or extra unreviewed changed path cannot approve; `maxApprovalManifestBytes` defaults to 16777216 and measures before Reviewer invocation the sum of byte lengths of all manifest-listed artifact contents that would be supplied, while `maxApprovalManifestPaths` defaults to 2000 and measures the distinct supplied paths, and overflow is an unrecoverable typed configuration failure that consumes no approval attempt and invokes no Reviewer; every boundary invokes a metered read-only Reviewer under REQ-AUTONOMOUS-DEVELOPMENT-004 and REQ-AUTONOMOUS-DEVELOPMENT-013 that is distinct from every invocation that produced the reviewed artifacts, receives only the frozen public specification, exact manifest-listed bytes, and stage evidence, carries no Planner or Developer conversation state, and returns REQ-AUTONOMOUS-DEVELOPMENT-010-validated typed findings plus an explicit reviewed-path set; an admissible finding cites a manifest path and a violated normative requirement or deterministic validator rule, while stylistic or informational notes are not findings; reviewer evidence is admissible only when its stage, boundary kind, boundary episode ordinal, amendment attempt ordinal when applicable, and originating nonce equal the current boundary, the reviewed-path set exactly equals the manifest path set, the invocation began after that nonce was issued, its recorded manifest digest equals the decision-time digest, and it reports zero admissible findings, and resolving a finding requires the authorized stage-producing role to create a new manifest digest followed by revalidation and re-review; `retry-same-manifest` means any consumed attempt caused by REQ-AUTONOMOUS-DEVELOPMENT-010 Reviewer exhaustion, transient Reviewer execution failure, or otherwise inadmissible Reviewer evidence without an admissible finding or candidate/release-evidence validator defect, including stage, boundary, ordinal, nonce, invocation-order, reviewed-path-set, or manifest-digest mismatch, and the identical manifest may be evaluated with a fresh nonce; `release-repair-required` means a release validator failure or admissible Reviewer finding attributable to candidate or release evidence consumed one boundary attempt and requires a newly accepted candidate with a newly frozen claim matrix and release-evidence manifest digest; `release-repair-required` exclusively invalidates the prior release-boundary validator, Reviewer, readiness-authority, frozen claim-matrix-authority, and gate evidence while retaining the old claim statuses only as the REQ-AUTONOMOUS-DEVELOPMENT-012 improvement and REQ-AUTONOMOUS-DEVELOPMENT-018 regression baseline, carries the release failure evidence into the next Planner invocation as an unresolved blocker, and re-enters the bounded Planner-Developer-QA path so the new candidate and claim matrix are revalidated at the same release boundary without resetting its approval-attempt counter; the requirements and design manifests include the effective run configuration and list exact reviewed paths and SHA-256 digests; a QA-dimension waiver is eligible only when frozen before run start in the public specification and effective configuration, covered by the initial requirements manifest, and unchanged at every later boundary, while any in-run addition, removal, or alteration of a waiver or dimension set is a typed configuration failure; a REQ-AUTONOMOUS-DEVELOPMENT-011 `amendment-required` stop requests operator action but does not itself enter the approval boundary; only the operator's explicit collision-inventory amendment request suspends the run at that boundary without advancing the iteration, generates the amendment manifest defined by REQ-AUTONOMOUS-DEVELOPMENT-013, and the replacement is rejected unless a verified automatic requirements approval reviews the replacement bytes and explicitly pins that amendment-manifest digest, after which the same frozen specification and requirement-index generation resumes at the same iteration; any other protected-set, public specification, or normalized requirement-index change requires a new run; an unrequested mismatch follows REQ-AUTONOMOUS-DEVELOPMENT-013 and cannot enter the amendment path; each evaluation attempt generates a fresh single-use nonce, and every automatic approval record contains the full boundary key, run ID, automated policy identity and policy digest, decision, timestamp, manifest digest, originating nonce, validator evidence, complete independent-review evidence, and mandatory-gate evidence; a record advances only when its nonce is the current unconsumed nonce and its manifest digest equals the bytes present at decision time; automatic evaluation permits at most `approvalBoundaryAttemptLimit` total attempts per keyed boundary, defaulting to 3 across all manifest digests; the durable counter is keyed by the full boundary identity, survives interruption, stop, resume, re-entry, byte changes, and nonce renewal, and a successful evaluation consumes 1 attempt then closes that boundary; invalid static approval configuration and manifest-size overflow fail before opening an attempt as unrecoverable typed configuration failures, while manifest-path mismatch, validator failure, REQ-AUTONOMOUS-DEVELOPMENT-010 exhaustion, or valid but inadmissible review evidence each consumes exactly 1 boundary attempt and permits the authorized producer to repair within the remaining bound; exhaustion records an unrecoverable typed `approval-blocked` failure, except an amendment-boundary block follows the counted REQ-AUTONOMOUS-DEVELOPMENT-012 amendment failure path; stale, missing, failed, skipped, rejected, replayed, manually malformed, digest-mismatched, incomplete-coverage, or insufficient evidence cannot approve; inherited explicit MUSUBIX3 approval records are ignored by MUSUBIX4 boundary evaluation and remain readable and re-emittable for compatibility without causing a configuration failure, but never satisfy a MUSUBIX4 run boundary or REQ-AUTONOMOUS-DEVELOPMENT-017; for the explicit Formal abstraction only, `approval.all-evidence-admissible` is false whenever any required validator, Reviewer, nonce, manifest, coverage, or release-gate evidence is stale, missing, failed, skipped, rejected, replayed, malformed, digest-mismatched, incomplete, or insufficient, and `approval.decision-approved` means an approval record whose decision is `approved`; consistency of this abstraction does not establish implementation behavior.
Formal: {"kind":"conditional","condition":"approval.all-evidence-admissible","conditionValue":false,"consequence":"approval.decision-approved","consequenceValue":false}

## REQ-AUTONOMOUS-DEVELOPMENT-017: Deploy and recover through configured commands / 設定済みデプロイ
Priority: must
Type: functional
Pattern: event-driven
Statement: When readiness and release approval are current, the system shall execute the configured deployment transaction including post-deploy verification and one rollback attempt after deployment or verification failure.
Acceptance: Deployment is impossible before readiness and current release approval; a deployment configuration without deploy, verification, and rollback commands is rejected before deployment starts; command provenance and bounded output are retained; successful deployment and verification record the deployed candidate digest; a failed or partially applied deployment command and a failed verification command each run rollback exactly once and record either `recovered` or `rollback-failed` terminal status.

## REQ-AUTONOMOUS-DEVELOPMENT-018: Roll back qualifying regressions / 回帰時のロールバック
Priority: must
Type: functional
Pattern: event-driven
Statement: When a regression occurs or a build blocker remains after the configured retry limit, the system shall base the next Developer iteration on the last preservation-verified candidate or the run base commit and retain the rejected candidate for diagnosis.
Acceptance: A regression is computed over the complete untruncated claim set and is a stable claim ID that changes from `verified` in a preceding iteration to `gap` or `insufficient-evidence`, any inherited compatibility check in REQ-AUTONOMOUS-DEVELOPMENT-001 or REQ-AUTONOMOUS-DEVELOPMENT-002 that changes from passing to failing, or any protected-set digest change not covered by a new current REQ-AUTONOMOUS-DEVELOPMENT-016 requirements approval; claim changes or run-level failures caused solely by a pre-claim REQ-AUTONOMOUS-DEVELOPMENT-011 `amendment-required` stop are excluded because no claims are assigned for that iteration, and the stop consumes neither `blockerRepairRetryLimit` nor stagnation budget; a preservation-verified candidate has passing configured build, focused tests, inherited compatibility checks, the approved protected-set digest, and 0 regressions; `blockerRepairRetryLimit` defaults to 2 retries; the run refuses to start if pre-existing user changes cannot be preserved; rollback changes only the isolated run workspace, restores the exact last preservation-verified tree or the run base commit when none exists, records the rejected candidate ref and triggering stable claim ID or blocker evidence, and never rewrites or checks out the user's branch.

## REQ-AUTONOMOUS-DEVELOPMENT-019: Keep release versions consistent / リリースバージョンの一貫性
Priority: must
Type: functional
Pattern: ubiquitous
Statement: The system shall expose one consistent semantic version across the executable, root and workspace package metadata, lockfile metadata, plugin metadata, marketplace metadata, release documentation, and packed distribution.
Acceptance: A deterministic test shall read `/version` from the root `package.json` as the authoritative value and report 0 mismatches against `/version` in each workspace manifest selected by the root `/workspaces` patterns; root `/version`, root-package entry `/packages//version`, and the corresponding `/packages/<escaped-workspace-path>/version` entry for every selected workspace in the root `package-lock.json`, which currently resolve to `/packages/packages~1analysis/version`, `/packages/packages~1cli/version`, and `/packages/packages~1domain/version`; the manifest workspace-path set and lockfile workspace-path set shall be equal so an added, missing, or unenumerated workspace fails verification; the test shall also report 0 mismatches against `/version` in `plugin.json`, `/metadata/version` and the `/plugins/*` entry named `musubix4` `/version` in `.github/plugin/marketplace.json`, and `musubix4 --version`. A declaration emitted by `recordWorkflow` shall carry the `/version` value from the musubix4 package root independently of the evidence-root path and process working directory; a deterministic test shall begin with an isolated temporary evidence root containing zero declarations, invoke `recordWorkflow`, and assert the sole newly emitted declaration version, while declarations already present in append-only evidence before an invocation are historical and excluded. Stable release headings in `CHANGELOG.md` shall be ordered by descending semantic version, the authoritative value shall equal the highest stable release heading, and the first `## <version> - <date>` release heading shall identify that value; `tests/fixtures/changelog-release-headings.json`, SHA-256 `a3837f22d4057c6bf8f887ca379a11739ec48940ab5a47603f32ea4b0fa21926`, shall be UTF-8 JSON with 2-space indentation, LF line endings, a trailing newline, top-level keys in the order `schemaVersion`, `headings`, `schemaVersion` equal to 1, and the sole heading `## 0.1.0 - 2026-09-16`; that immutable heading sequence shall remain the contiguous trailing block of the current release-heading sequence so later releases can only prepend newer dated stable headings; the deterministic test shall parse the approved digest from this requirement and fail when the fixture digest differs; changing the baseline fixture or its approved digest requires a separately approved requirement change; the leading `**Latest release v...` marker in `README.md` and the leading `**最新リリース v...` marker in `README-ja.md` shall identify the authoritative value, while historical version references elsewhere in those documents and historical append-only workflow declarations are excluded; the package verification command shall read `package.json`, `plugin.json`, `.github/plugin/marketplace.json`, `README.md`, `README-ja.md`, and the first release heading in `CHANGELOG.md` from inside the generated archive and report 0 mismatches against the authoritative root version; the `musubix4 --version` obligation complements the product-identity requirement in REQ-AUTONOMOUS-DEVELOPMENT-001.
