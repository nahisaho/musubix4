---
schemaVersion: 1
feature: safe-workflow-speed
---
# Safe workflow speed design / 安全なワークフロー高速化設計

## DES-SAFE-WORKFLOW-SPEED-001: Concise validation output
Responsibilities: Define a shared Skill-level allowlist for concise human-readable validation and a required-JSON set for structured decisions.
Interfaces: `.github/skills/sdd-change/SKILL.md`; `.github/skills/sdd-requirements/SKILL.md`; `.github/skills/sdd-design/SKILL.md`; `.github/skills/sdd-implementation/SKILL.md`; `.github/skills/sdd-traceability/SKILL.md`; `.github/skills/sdd-quality/SKILL.md`; `tests/cli-package.test.ts`.
Constraints: Human-readable output is used only for the commands enumerated by REQ-SAFE-WORKFLOW-SPEED-001 and only when no result field must be parsed; this includes concise `approval validate`. `gate` is human-readable when only its exit/status summary is consumed and uses JSON when individual checks feed evidence, readiness, or waiver decisions. HoH lifecycle commands use bounded summary JSON; approval preparation, workflow verification, readiness and waiver decisions retain structured output on their first invocation. Mutating and append-only commands are never rerun only for diagnostics; written evidence or the original result is inspected. Summary-less commands are narrowly scoped and excluded from the default completion path unless required.
Requirements: REQ-SAFE-WORKFLOW-SPEED-001
ADRs: ADR-0011

## DES-SAFE-WORKFLOW-SPEED-002: Dependency-aware validation batching
Responsibilities: Instruct Copilot to submit only the requirement-enumerated independent read-only validators in one parallel tool-call batch while preserving all stateful and producer-consumer order.
Interfaces: The six shipped SDD Skills named by DES-SAFE-WORKFLOW-SPEED-001; Copilot native parallel tool calls; `tests/cli-package.test.ts`.
Constraints: Requirements approval precedes design, design approval precedes Red, Red precedes implementation, and implementation precedes Green. `trace build` precedes `trace check` and `trace impact`; graph indexing precedes graph consumers. The parallel allowlist is limited to requirements plus constitution validation, distinct design-file validations, and current-artifact trace/config/mutation/model-correspondence/approval validation. Producers, HoH commands, project build/test commands, gates, status, approval preparation and recording, TDD, change/workflow recording, waivers, and other shared or append-only writes remain sequential.
Requirements: REQ-SAFE-WORKFLOW-SPEED-001
ADRs: ADR-0011
Depends-On: DES-SAFE-WORKFLOW-SPEED-001

## DES-SAFE-WORKFLOW-SPEED-003: Bilingual batching disclosure
Responsibilities: Publish the complete safe parallel allowlist, sequential command boundary, and required lifecycle order in both release entry points, with deterministic regression coverage.
Interfaces: `README.md`; `README-ja.md`; `tests/release-documents.test.ts`; `TEST-SAFE-WORKFLOW-SPEED-003`, retargeted to `REQ-SAFE-WORKFLOW-SPEED-002` and strengthened.
Constraints: Replace the existing batching paragraph under `## Workflow` and `## ワークフロー`; each section contains exactly one contiguous batching-boundary passage, identified respectively by the exact anchors `Only the explicitly allowlisted independent read-only validators` and `明示的に許可された独立した読み取り専用validatorだけ`, before the next `##` heading. CLI command identifiers remain backticked English literals in both documents, while surrounding prose and test assertions use each document's own language. The passage names every parallel-eligible validator and states that all other commands remain sequential, explicitly including artifact producers (`trace build`, `graph index`, `knowledge build`, `evidence refresh`), consumers outside the allowlist or with stale inputs, gates, `workflow-verify`, `workflow-record`, workflow waivers, approval preparation and recording, TDD commands, change-record commands, HoH lifecycle commands, `status`, project build/test commands, and append-only workflow/change/approval/TDD/HoH journal writes. It states all six required orderings: requirements approval before design, design approval before Red, Red before implementation, implementation before Green, `trace build` before trace consumers, and `graph index` before graph consumers. `TEST-SAFE-WORKFLOW-SPEED-003` checks one anchor occurrence at section scope, extracts the paragraph from that anchor to the following blank line, normalizes whitespace, and asserts every required command/category, all six ordering pairs, and English or Japanese boundary prose within that passage rather than elsewhere in the section or file.
Requirements: REQ-SAFE-WORKFLOW-SPEED-002
ADRs: ADR-0011
Depends-On: DES-SAFE-WORKFLOW-SPEED-002

## DES-SAFE-WORKFLOW-SPEED-004: Identity-bound code-graph cache
Responsibilities: Reuse only an exact fresh persisted code graph, preserve complete rebuild semantics for invalid or forced inputs, and expose deterministic per-invocation indexing operations.
Interfaces: `packages/analysis/src/graph.ts#indexGraph`; `packages/analysis/src/graph.ts#loadGraph`; `packages/analysis/src/graph.ts#graphInputs`; `packages/analysis/src/package-version.ts#currentPackageVersion`; `packages/analysis/src/change.ts`; `packages/analysis/src/gate.ts`; `packages/cli/src/main.ts`; `.musubix/cache/codegraph.json`; `tests/trace-graph.test.ts`; `tests/safe-workflow-performance.test.ts`.
Constraints: Advance `CodeGraph.schemaVersion` to 2 and add a producer object containing the installed musubix4 package version, a manually advanced graph-extractor algorithm version, a package-root-relative SHA-256 over the executing extractor's complete first-party transitive module closure, and `ts.version`. Discover the closure with TypeScript preprocessing of static first-party imports and exports starting at `import.meta.url`: built `.js` execution resolves exact URL-relative `.js` files, while repository source execution may map a missing relative `.js` target to the corresponding `.ts` or `.tsx` sibling under the same package source root; recurse only within the named musubix4 package root, normalize every digest path relative to that root, sort paths bytewise, and fail with the importing module and specifier when a first-party member is unresolved or unreadable rather than skipping it. Separate indexed graph paths from fingerprint-only inputs; include `package-lock.json`, `npm-shrinkwrap.json`, `pnpm-lock.yaml`, `yarn.lock`, `bun.lock`, `bun.lockb`, `Cargo.lock`, `go.sum`, `poetry.lock`, `uv.lock`, `Gemfile.lock`, `composer.lock`, `Package.resolved`, and `pubspec.lock` only in the fingerprint set. Introduce `GraphCachePolicy = "read-write" | "cache-read-only" | "refresh" | "bypass"` and `GraphIndexOptions { cachePolicy, operations? }`; preserve `indexGraph(root, true)` as `read-write` and `indexGraph(root, false)` as `cache-read-only`. Compute current inputs and fingerprints once, compare canonical sorted path/digest entries, and return a typed fresh/unusable cache decision shared by indexing and strict consumers. `read-write` reuses fresh content or rebuilds once and writes; `cache-read-only` reuses fresh content or rebuilds once without writing; `refresh` does not read and always rebuilds once and atomically writes; `bypass` neither reads nor writes and always rebuilds once. `graph index --changed` selects `refresh`; normal `graph index`, `graph gate`, and `gate` select `read-write`; change evidence retains the boolean false compatibility call; `loadGraph` uses the validator but converts every unusable result into the existing actionable rebuild error. Keep `writeJson` as the only persistence primitive and propagate write failures. Increment a caller-owned `GraphIndexOperations.typescriptProgramBuilds` immediately before `ts.createProgram`; never use module-global telemetry. Cache reuse returns the stored graph unchanged, preserves construction-time `generatedAt`, and performs no write. Tests compare reuse with a complete bypass rebuild after excluding `generatedAt`, prove exact cache-byte invariants, and cover source/dist closure resolution and failures, refresh, present/absent bypass, schema/producer/input/lockfile invalidation, strict-consumer rejection, and exactly one construction on every rebuild path.
Requirements: REQ-SAFE-WORKFLOW-SPEED-003
ADRs: ADR-0016
Depends-On: DES-SAFE-WORKFLOW-SPEED-001

## DES-SAFE-WORKFLOW-SPEED-005: Nonce-bound operation evidence
Responsibilities: Attach the measured warm-cache operation count to the one authoritative full-test result without adding or changing a configured policy command.
Interfaces: `scripts/run-full-tests.mjs`; `.musubix/config.json`; `.musubix/policy-baseline.json`; `.test-work`; `tests/safe-workflow-performance.test.ts`; `scripts/run-hoh-tdd.mjs`; `TEST-SAFE-WORKFLOW-SPEED-004`.
Constraints: Keep the `test` command definitions in `.musubix/config.json` and `.musubix/policy-baseline.json` byte-identical. Before spawning Vitest, `run-full-tests.mjs` creates an unpredictable nonce and unique sidecar path below `.test-work`, removes any pre-existing path, and passes both through dedicated environment variables. The TEST-SAFE-WORKFLOW-SPEED-004 test warms through the public read-write API, measures the second invocation with a new recorder, asserts `typescriptProgramBuilds === 0`, validates refresh and bypass using separate recorders whose counts are not reported, verifies the benchmark artifact, and writes `{schemaVersion, nonce, testId, operations}` atomically to the sidecar only after all assertions pass. After successful Vitest completion, the runner requires exactly one existing result and one `testFiles` binding for the test ID, validates the current nonce and exact operation schema, augments that existing result, and emits no synthetic entry. Missing, unreadable, malformed, stale-nonce, duplicate, or non-executed evidence fails with the sidecar path and `node scripts/run-full-tests.mjs --report <path>` regeneration command; the finalizer removes the sidecar on success or failure. TDD and every report producer other than the full-test runner omit `operations`. Register the test file in the existing HoH TDD allowlist.
Requirements: REQ-SAFE-WORKFLOW-SPEED-003
ADRs: ADR-0016
Depends-On: DES-SAFE-WORKFLOW-SPEED-004

## DES-SAFE-WORKFLOW-SPEED-006: Pinned comparative benchmark
Responsibilities: Produce reproducible process-level performance evidence against the approved target and reject stale, unsuccessful, or non-comparable results.
Interfaces: `.musubix/benchmarks/graph-cache-target.json`; `.musubix/evidence/graph-performance.json`; `scripts/benchmark-graph-cache.mjs`; `tests/safe-workflow-performance.test.ts`; Git worktrees; npm lockfile installation; v0.1.2 and preceding-stable package builds.
Constraints: Parse and schema-check the pin, require its file SHA-256 to equal the digest embedded in REQ-SAFE-WORKFLOW-SPEED-003, and independently recompute the pinned commit's sorted Git-blob target digest and raw lockfile digest before creating benchmark copies. Resolve the improvement reference as exactly v0.1.2 and the regression reference as the highest stable CHANGELOG version lower than the candidate, deduplicating their execution when equal. Materialize each reference source and CLI build in its own detached worktree from the exact annotated or lightweight release tag `v<version>`, require the tag commit to be reachable, compute the reference performance-surface digest from that checkout, install with its lockfile, and build there; materialize the candidate build from the current approved source tree. Independently create one fresh analysis-target worktree from the pinned target commit per distinct CLI build, verify target and lockfile digests before installation, install from the pinned target lockfile, and invoke only the corresponding external CLI build against that target cwd. Compute reference performance-surface digests from the four named tracked TypeScript roots and their first-party transitive source closures; compute the candidate digest from the same closure plus the benchmark script, both relative to repository root with sorted path/NUL/raw-byte/NUL hashing. Require candidate digest equality before build and after all samples, build immediately before measurement, and fail on missing sources or build drift. Use one fixed human-readable command table, identically ordered for every build: `requirements validate .musubix/features/safe-workflow-speed/requirements.md`; `design validate .musubix/features/safe-workflow-speed/design.md`; `trace build`; `trace check --strict`; `graph index`; `graph gate`; `graph impact packages/analysis/src/graph.ts`; `status`; and `knowledge build`. Record the normalized executable and complete argument vector for every warm-up and sample and reject any cross-build command-table difference other than executable location. Execute every install, warm-up, and sample with bounded argument-array subprocesses, require exit 0, and record exit codes. For each target copy, run exactly one cache-warming `graph index` before the first measured command, discard only that invocation, then run five samples of every command without refresh or bypass interleaving. Compute medians and ratios, require warm `graph index` and `graph gate` at most 80 percent of v0.1.2 and every named non-regression command at most 105 percent of the preceding stable reference, and exclude full `gate`. Remove any previous artifact before starting and atomically write the current run under the versioned schema whether thresholds pass or fail; a process/setup failure writes a failing artifact with completed records and an actionable diagnostic when schema construction remains possible, otherwise leaves no artifact, and the benchmark exits nonzero unless all requirements pass. TEST-SAFE-WORKFLOW-SPEED-004 validates schema, pass flags, exact versions, candidate source digest, pin identity, command vectors, all sample counts and zero exits, recomputed medians and ratios, and emits an actionable benchmark regeneration command on failure without running the benchmark.
Requirements: REQ-SAFE-WORKFLOW-SPEED-003
ADRs: ADR-0016
Depends-On: DES-SAFE-WORKFLOW-SPEED-004 DES-SAFE-WORKFLOW-SPEED-005
